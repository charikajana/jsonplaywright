# Browser Configuration
browser.type=chromium
browser.headless=false
browser.timeout=60000
browser.slowmo=0

# Cache Configuration
cache.directory=cache/pages
cache.validation.enabled=true
cache.autohealing.enabled=false

# Reporting Configuration
reporting.enabled=true
reporting.screenshots.enabled=true
reporting.screenshots.onfailure=true
reporting.agent.decisions=false
